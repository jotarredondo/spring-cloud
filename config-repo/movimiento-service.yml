server:
  port: 8082

spring:
  application:
    name: movimiento-service

  datasource:
    url: jdbc:mysql://localhost:3306/banco_bff?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: user_ADMIN1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

mensaje:
  prueba: Movimiento Service configurado desde Config Server